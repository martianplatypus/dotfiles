# show colors in the terminal
export CLICOLOR=1
export TERM=xterm-256color

# History
export HISTCONTROL=ignoredups
export HISTIGNORE="ls:cd:pwd:history:exit:clear"
export HISTTIMEFORMAT="%h %d %H:%M:%S "
export HISTFILESIZE=10000

shopt -s histappend

#GPG
export GPG_TTY=`tty`
